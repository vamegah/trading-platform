def calculate_carry(root_symbol: str) -> dict[str, str | float]:
    return {
        "root_symbol": root_symbol.upper(),
        "annualized_carry": -0.036,
        "recommendation": "avoid_long_roll",
    }

